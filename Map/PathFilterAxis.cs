namespace PathPlanner.Map;

/// <summary>
/// Which path statistic the map overlay filters on (exact count match).
/// </summary>
public enum PathFilterAxis
{
    Any,
    Elites,
    Shops,
    Rests,
}
